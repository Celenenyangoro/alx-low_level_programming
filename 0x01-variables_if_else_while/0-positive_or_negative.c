#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main -block
 * Return: 0
 */
int main (void)

{
	int n;
	srand(time(0));
	n-rand() - RAND_MAX/2;
	lf (n<0)

{
	printf(-Xd is negative\n-,n);
}	else if (n--0)
{
	printf(-Xd is zero\n",n)
}else
{
	printf(Xd is positive\n-,n);
}
return(0);
}
